# Seed values are advisory and may lag; refresh via `sluice models update` (design §9.6).
#
# Anthropic serving overlay for Claude Sonnet 4.5: inherits context/output
# limits and modalities from ../../models/claude-sonnet-4-5-base.toml via
# `base_model`, adds cost/status/tool_call.

base_model = "claude-sonnet-4-5-base"
status = "stable"
tool_call = true

[cost]
input = 3.0
output = 15.0
